/**
 * Vditor Plugin Checker
 */

/**
 * Vditor Plugin Compatible Checker
 * @param compatible Vditor Plugin Compatible Version
 * @param vditor_version Current Vditor Version
 *
 * Rules:
 * version
 * >version
 * <version
 * >=version
 * <=version
 * ~version
 * ^version
 * *
 * version1 - version2
 * range1 || range2
 */
export const checkVditorPluginCompatible = (compatible: string, vditor_version: string) => {

}
